<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hubilo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --red: #dc3545; --dark-red: #b02a37; }
        body { font-family: 'Segoe UI', sans-serif; margin: 0; }
        .split { display: flex; min-height: 100vh; }
        .side { flex: 1; padding: 60px 50px; display: flex; flex-direction: column; justify-content: center; position: relative; }
        .login { background: linear-gradient(135deg, #fff 0%, #f8f9fa 100%); color: #333; }
        .signup { background: linear-gradient(135deg, var(--red) 0%, var(--dark-red) 100%); color: white; }
        .title { font-size: 2.8rem; font-weight: 800; margin-bottom: 20px; }
        .login .title { color: var(--red); }
        .desc { font-size: 1.1rem; margin-bottom: 40px; line-height: 1.6; max-width: 500px; }
        .btn-side { padding: 20px 30px; margin: 12px 0; border-radius: 12px; font-size: 1.2rem; font-weight: 600; text-decoration: none; display: block; transition: all 0.3s; }
        .login .btn-side { background: white; color: #333; border: 2px solid #ffdce0; }
        .login .btn-side:hover { background: #fff5f5; color: var(--red); border-color: var(--red); transform: translateY(-3px); }
        .signup .btn-side { background: rgba(255,255,255,0.1); color: white; border: 2px solid rgba(255,255,255,0.3); }
        .signup .btn-side:hover { background: rgba(255,255,255,0.2); transform: translateY(-3px); }
        @media (max-width: 768px) { .split { flex-direction: column; } .side { padding: 40px 30px; } .title { font-size: 2.2rem; } }
    </style>
</head>
<body>
    <div class="split">
    <div class="side login">
        <h1 class="title">Welcome Back</h1>
        <p class="desc">Sign in to continue your journey with Hubilo.</p>
        
        <!-- Use /users/login for viewers -->
        <a href="/login" class="btn-side"><i class="fas fa-eye me-2"></i> Login as Viewer</a>
        <a href="/owner/login" class="btn-side"><i class="fas fa-home me-2"></i> Login as Owner</a>
        
    </div>
    <div class="side signup">
        <h1 class="title">Join Our Community</h1>
        <p class="desc">Create an account to start your journey.</p>
        
        <!-- Use /users/signup for viewers -->
        <a href="/signup" class="btn-side"><i class="fas fa-user me-2"></i> Signup as Viewer</a>
        <a href="/owner/signup" class="btn-side"><i class="fas fa-building me-2"></i> Signup as Owner</a>
    </div>
</div>
    </div>
</body>
</html>